import { expect } from "chai";

describe("It must do basic arithmatic", () => {
  describe("It must add", () => {
    describe("It must add two numbers", () => {
      expect(formuala("= 1 + 2 ")).to.equal(3);
    });
  });
});
